En la solución de las prácticas se encuentra además un archivo con código para comprobar automáticamente el funcionamiento de la práctica. Simplemente se debe escribir en la consola de tipo Unix que se use el comando "./nombre_del_archivo".

P1: Mytar/pruebaCodigo.sh -> por tanto se debe acceder a la carpeta Mytar y escribir ./pruebaCodigo.sh

P2: FUSE_myFS/scriptP2.sh

P3: No tiene archivo .sh pero se puede ejecutar el Makefile ya que simplemente es un ejercicio de mutex y variables condicionales.
